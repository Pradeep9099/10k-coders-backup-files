import React from 'react'

const UserDetails = ({allUsers}) => {
  return (
    <div>
        <h2>Hello From UserDetails</h2>
        {/* <ul>
            {allUsers.map((user,i)=> <li key={i}>{user}</li>)}
        </ul> */}
    </div>
  )
}


export default UserDetails; 